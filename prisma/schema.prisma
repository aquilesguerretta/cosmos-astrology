// TODO: Prompt 9 — full schema (User, Account, Session, SavedChart, HoroscopeCache)

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
