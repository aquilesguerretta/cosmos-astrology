export default function OnboardingPage() {
  // TODO: Prompt 9 — multi-step natal data capture (name → date → time + city → preview)
  return (
    <main className="min-h-screen grid place-items-center">
      <p className="label-caps text-[var(--text-muted-color)]">TODO · /onboarding</p>
    </main>
  );
}
