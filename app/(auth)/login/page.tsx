export default function LoginPage() {
  // TODO: Prompt 9 — auth flow (Google OAuth + Credentials), see figma-export glassmorphism styling
  return (
    <main className="min-h-screen grid place-items-center">
      <p className="label-caps text-[var(--text-muted-color)]">TODO · /login</p>
    </main>
  );
}
